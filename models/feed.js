const mongoose = require('mongoose'),
      Blog     = require('./blog');

class Feed {
  
  getPosts(friends) {
    let posts = [];

    for (friend in friends) {
      
    }
  }

}

module.exports = Feed;